services:
  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=entroq
      - POSTGRES_USER=entroq
      - POSTGRES_PASSWORD=entroq
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U entroq -d entroq"]
      interval: 5s
      timeout: 5s
      retries: 5

  entroq:
    build:
      context: ../..
      dockerfile: ./cmd/eqpg/Dockerfile
    environment:
      - PGPASSWORD=entroq
    ports:
      - "37706:37706"
      - "9100:9100"
    depends_on:
      postgres:
        condition: service_healthy
      opa:
        condition: service_started
    command:
      - "serve"
      - "--dbaddr=postgres:5432"
      - "--dbname=entroq"
      - "--dbuser=entroq"
      - "--heartbeat=5s"
      - "--init_schema"
      - "--authz=opahttp"
      - "--opa_url=http://opa:8181"

  opa:
    image: openpolicyagent/opa:latest
    command:
      - "run"
      - "--addr=:8181"
      - "--server"
      - "--watch"
      - "--log-level=info"
      - "/etc/opa/core"
      - "/etc/opa/permissions"
      - "/etc/opa/local"

    ports:
      - "8181:8181"
    volumes:
      # Core EntroQ Rego -- do not modify.
      - ../../pkg/authz/opadata/conf/core:/etc/opa/core:ro
      # Built-in standalone/OIDC permissions provider -- do not modify.
      - ../../pkg/authz/opadata/conf/providers/entroq/permissions:/etc/opa/permissions:ro
      # Example config: inline-JWKS user provider plus users/roles policy.
      - ./opa:/etc/opa/local:ro
